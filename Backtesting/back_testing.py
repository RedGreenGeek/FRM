


def back_testing(df_data):

    var99s = df_data.columns.str.contains('0.99')

    var95 = df_data.columns.str.contains('0.95')
    
    inv+'_USD' if '.DE' in inv or '.L' in inv else inv for inv in investments 

    all(x in 'tomato' for x in ['t','o','m','a'])

